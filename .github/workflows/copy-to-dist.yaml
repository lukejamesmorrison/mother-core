name: Build and Release

on:
  push:
    tags:
      - 'v*'   # Only run when pushing tags like v1.0.0

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repo
      - uses: actions/checkout@v4

      # 2. Build dist folder
      - name: Build dist folder
        run: |
          mkdir -p dist
          cp src/Mother.cs dist/
          cp src/.gitignore dist/
          cp src/.gitattributes dist/

          cp -r src/Commands dist/
          cp -r src/Modules dist/
          cp -r src/Utilities dist/

          cp CHANGELOG.md dist/
          cp LICENSE.md dist/
          cp README.md dist/

      # 3. Create a zip archive of the *contents* of dist
      - name: Archive dist contents
        run: |
          cd dist
          zip -r ../mother-core.zip .

      # 4. Create GitHub Release from the tag
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: mother-core.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
